menu "AHT20 Sensor Configuration"

    config AHT_I2C_PORT_NUM
        int "I2C Port Number"
        default 1
        help
            I2C port number for AHT20 sensor (0 or 1). 
            Since SSD1306 uses Port 0, keep this as 1.

    config AHT_SDA_GPIO
        int "AHT20 SDA GPIO"
        default 10
        help
            GPIO number for AHT20 SDA signal.

    config AHT_SCL_GPIO
        int "AHT20 SCL GPIO"
        default 11
        help
            GPIO number for AHT20 SCL signal.

endmenu